# =============================================================================
# RAG Cookbooks - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the actual .env file with real credentials to git
# =============================================================================

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration (Required for most notebooks)
# -----------------------------------------------------------------------------
# Get these from your Azure OpenAI resource in the Azure portal
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
OPENAI_API_VERSION=2024-02-01

# Alternative: Standard OpenAI (if using OpenAI instead of Azure)
# OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# Vector Database Configuration (Choose based on notebook)
# -----------------------------------------------------------------------------

# Pinecone (used in some notebooks)
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=your_pinecone_environment

# Qdrant (used in fusion_rag.ipynb)
QDRANT_URL=your_qdrant_cluster_url
QDRANT_API_KEY=your_qdrant_api_key

# Weaviate (used in hyde_rag.ipynb)
WEAVIATE_URL=your_weaviate_cluster_url
WEAVIATE_API_KEY=your_weaviate_api_key

# -----------------------------------------------------------------------------
# RAG Evaluation and Monitoring
# -----------------------------------------------------------------------------
# Athina AI (used for RAG evaluation in multiple notebooks)
ATHINA_API_KEY=your_athina_api_key_here

# LangSmith (used for debugging and monitoring)
LANGCHAIN_API_KEY=your_langsmith_api_key_here
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=rag-cookbooks

# -----------------------------------------------------------------------------
# Document Intelligence (for PDF processing)
# -----------------------------------------------------------------------------
# Azure Document Intelligence (used for PDF to markdown conversion)
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-doc-intel-resource.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_KEY=your_document_intelligence_key_here

# -----------------------------------------------------------------------------
# Optional: Additional Services
# -----------------------------------------------------------------------------
# Hugging Face (for alternative models)
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Cohere (for alternative embeddings)
COHERE_API_KEY=your_cohere_api_key_here

# Anthropic (for alternative LLMs)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Set to 'development' for local testing, 'production' for deployment
ENVIRONMENT=development

# Enable debug logging (true/false)
DEBUG=true

# Rate limiting settings (requests per minute)
RATE_LIMIT_RPM=60

# Document processing limits (to avoid hitting API quotas during testing)
MAX_DOCUMENTS=20
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# -----------------------------------------------------------------------------
# Usage Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys and endpoints
# 3. Add .env to your .gitignore file (already included)
# 4. Never commit the actual .env file to version control
# 5. Each notebook may use different services - only fill in what you need
#
# Required for each notebook:
# - naive_rag.ipynb: AZURE_OPENAI_* (FAISS is local, no API key needed)
# - contextual_rag.ipynb: AZURE_OPENAI_*, ATHINA_API_KEY (ChromaDB is local)
# - fusion_rag.ipynb: AZURE_OPENAI_*, QDRANT_*, ATHINA_API_KEY, LANGCHAIN_*
# - hybrid_rag.ipynb: AZURE_OPENAI_*, ATHINA_API_KEY (ChromaDB is local)
# - hyde_rag.ipynb: AZURE_OPENAI_*, WEAVIATE_*, ATHINA_API_KEY
# - parent_document_retriever.ipynb: AZURE_OPENAI_*, ATHINA_API_KEY (ChromaDB is local)
# - rewrite_retrieve_read.ipynb: AZURE_OPENAI_*, ATHINA_API_KEY (ChromaDB is local)
# - basic_unstructured_rag.ipynb: AZURE_OPENAI_*, ATHINA_API_KEY (FAISS is local)
# =============================================================================
